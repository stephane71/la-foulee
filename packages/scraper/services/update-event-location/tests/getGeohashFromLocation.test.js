// Problem: handle es6 module with Jest
/*const getGeohashFromLocation = require("../getGeohashFromLocation");

describe("scraper:update-event-location:getGeohashFromLocation", () => {
  test("Should return null on falsy argument", () => {
    expect(getGeohashFromLocation("")).toEqual(null);
    expect(getGeohashFromLocation()).toEqual(null);
    expect(getGeohashFromLocation(null)).toEqual(null);
  });
});*/
